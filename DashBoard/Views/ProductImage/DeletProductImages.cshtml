@model ProductImagesViewModel
@{
    ViewData["Title"] = "DeletProductImages";
}

<div style="display: flex; justify-content: center; align-items: center; height: 100vh;">
    <div style="text-align: center; border: 1px solid #ccc; padding: 30px; border-radius: 10px; background-color: #f8f8f8;">

        <h3>Do you want to delete this item?</h3>
        <img src="@Model.ImageUrl" alt="Product Image" style="width: 60px; height: 50px; cursor: pointer;" />

        <form asp-action="DeletProductImagesfn" asp-controller="ProductImage" method="post">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger">yse</button>
            <a asp-action="GetAllSubCategrey" asp-controller="SubCategrey" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</div>

