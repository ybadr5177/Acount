@model IEnumerable<SubCategoryViewModel>
@{
    Layout = "_LayoutTheOtherPage";
    ViewData["Title"] = "GetAllSubCategory";
}

<h2>SubCategory</h2>

<h2 style="text-align:center; margin: 20px 0;">عرض الفئات</h2>

@* <div class="slider-container">
    <button class="slider-btn left-btn">&#8592;</button>

    <div class="category-slider" id="categorySlider">

        @foreach (var item in Model)
        {
            <a asp-controller="Mastar" asp-action="GetAllSubCategory" asp-route-id="@item.Id"
               class="category-link">
                <div class="category-item">
                    <img src=@item.Picture alt="@item.Name_EN" />
                    <div class="category-name">@item.Name_EN</div>
                </div>
            </a>
        }


        <!-- يمكنك إضافة فئات إضافية -->
    </div>

    <button class="slider-btn right-btn" disabled>&#8594;</button>
</div>
<script src="~/js/display-category.js"></script> *@
<div class="sec-banner bg0 p-t-80 p-b-50">
    <div class="container">
        <div class="row">

            <!-- Block1 -->
            @foreach (var item in Model)
            {
                <div class="col-md-6 col-xl-4 p-b-30 m-lr-auto">


                    <div class="block1 wrap-pic-w">
                        <img src=@item.Picture alt="IMG-BANNER">

                        <a asp-controller="Mastar" asp-action="Index" asp-route-SubCategorysId="@item.Id" class="block1-txt ab-t-l s-full flex-col-l-sb p-lr-38 p-tb-34 trans-03 respon3">
                            <div class="block1-txt-child1 flex-col-l">
                                <span class="block1-name ltext-102 trans-04 p-b-8">
                                    @item.Name_EN
                                </span>

                            </div>

                            <div class="block1-txt-child2 p-b-4 trans-05">
                                <div class="block1-link stext-101 cl0 trans-09">
                                    Shop Now
                                </div>
                            </div>
                        </a>
                    </div>
                </div>
            }

        </div>
    </div>
</div>
